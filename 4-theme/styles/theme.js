const theme = {
  colors: {
    // The naming convention here aligns *exactly* with the names UX use for colours
    sounds: {
      core: "#fa6400",
      dark: "#d24712",
    },
    live: {
      core: "#00a5a5",
      dark: "#00857f",
    },
    grey: {
      1: "#fefefe",
    },
  },
  // What else could we put in our theme object?
  // Some ideas: typography, spacing, borders, opacities, transitions...
};

export default theme;
